<wxs module="fps" src="../../../../ij.sjs" /><include src="../../../../er.ttml" /><wd show-capsuale="{{false}}" id="coupon-nav" content-style="position: fixed; background-color: #fff;}}" bind:passNavHeight="passNavHeight" padding-bottom="{{0}}"><view class="nav-custom"><view class="back-icon" bindtap="goBack"></view><view class="title-text" style="position: absolute;left: 50%;transform: translateX(-50%);">{{'优惠券'}}</view></view><view class="swiper-tab"><ef class="tab-item" padding="-6" active="{{tab===item}}" data-current="{{item}}" font-size="30" bindtap="tabClick" tt:for="{{[1,2,3]}}" tt:for-item="item" tt:key="tab-{{index}}">{{COPYWRITING[item]}}</ef></view><eh detail="{{noticeBoardInfo}}" tt:if="{{isShowMember}}" /></wd><view class="coupon-body" style="top:{{top}}rpx;" capture-bind:touchstart="__fstBindCaptureTouchStart"><view class="coupon-list"><block tt:if="{{(!validCouponList.length && tab === CURRENTSTATUS.NO_USE) || (!recmmondCouponList.length&& !usedCouponList.length&&tab===CURRENTSTATUS.USED || !recmmondCouponList.length &&!invalidCouponList.length&&tab===CURRENTSTATUS.INVALID)}}"><wm tips="暂无优惠券" type="COUPON" /></block><eg id="coupon-valid" tab="{{tab}}" CouponLists="{{validCouponList}}" isGetRemmond="{{isGetRemmond}}" recmmondCouponList="{{recmmondCouponList}}" total="{{validTotal}}" bindchangeRec='newRecGoods' bindopenCityScopePop="toogleCityScopePop" invalidLabel="{{invalidLabel}}" tt:key="tab1" tt:if="{{tab === CURRENTSTATUS.NO_USE}}" /><eg id="coupon-used" tab="{{tab}}" CouponLists="{{usedCouponList}}" isGetRemmond="{{isGetRemmond}}" recmmondCouponList="{{recmmondCouponList}}" total="{{usedTotal}}" bindchangeRec='newRecGoods' bindopenCityScopePop="toogleCityScopePop" tt:key="tab2" tt:if="{{tab === CURRENTSTATUS.USED}}" /><eg id="coupon-invalid" tab="{{tab}}" CouponLists="{{invalidCouponList}}" isGetRemmond="{{isGetRemmond}}" recmmondCouponList="{{recmmondCouponList}}" total="{{invalidTotal}}" bindchangeRec='newRecGoods' bindopenCityScopePop="toogleCityScopePop" tt:key="tab3" tt:if="{{tab === CURRENTSTATUS.INVALID}}" /></view><view class="__fst_judge_target" style="display:hidden;" /></view><ej show="{{showCityScopePop}}" cities="{{cityScope}}" bindclosePop="toogleCityScopePop"></ej><view change:prop="{{fps.fpsRecord}}" prop="{{_fpsIsComputed}}"></view>