<wxs module="fps" src="../../../../ij.sjs" /><include src="../../../../er.ttml" /><import src="../../ow.ttml" /><import src="../../../../n.ttml" /><ww bottom-brand-options="{{({isHide: !loadEnd || !skuList.length , backgroundColor: '#fff'})}}" add-cart-bar-options="{{({cid: 'c_youxuan_78tprjcj', queryParams})}}" showAddCartBar="{{true}}" mv-handler="{{__mvHandler}}" isShowRedPacket="{{false}}"><view class="container" capture-bind:touchstart="__fstBindCaptureTouchStart"><view class="sku-filter"><ep id="filter" is-coupon="{{true}}" promotion-id="{{couponId}}" couponCode="{{couponUserId}}" bind:search="search" extensions="{{extensions}}" has-title="{{titleList.length}}"><block tt:if="{{titleList.length&&skuList.length}}"><view class="coupon-title"><template is="wxParse" data="{{wxParseData: titleList[0].val}}"></template></view></block></ep></view><block tt:if="{{titleList.length&&skuList.length}}"><view class="coupon-subtitle">{{titleList[1].val}}</view></block><view class="sku-list-wrapper {{loadEnd ? 'isEnd' : ''}}" tt:if="{{skuList.length}}"><ei newCardSchema="{{schema}}" skuType="{{skuType}}" is-show-spec-price="{{true}}" initial-value="{{skuList}}" show-cart-icon="{{false}}" sku-proxy-data="{{skuProxyData}}" module-id="5" current-container-class="sku-list-wrapper" bind:onCartChange="onCartChange"></ei><template is="loading" data="{{loadingType:'circle', class: 'bottom-loading', show: !loadEnd && skuList.length}}" tt:if="{{loading.show}}"></template></view><wm tt:if="{{!firstLoading && skuList.length==0}}"></wm><t id="bitc-toast"></t><view class="__fst_judge_target" style="display:hidden;" /></view></ww><view change:prop="{{fps.fpsRecord}}" prop="{{_fpsIsComputed}}"></view>