<oe external-class="round {{item.type}}" tt:for="{{mapList || []}}" tt:key="index"><oi status="{{statusCard}}" isFixed="{{isFixed}}" navigatorHeight="{{navigatorHeight}}" order-info="{{orderInfo}}" tt:if="{{item.type === 'cardStatus'}}" /><ou can-show="{{canShowQr}}" order-status="{{orderStatus}}" self-lift-info="{{selfLiftInfo}}" distribution-info="{{distributionInfo}}" tt:if="{{item.type === 'distributionInfo'}}" /><oo order-info="{{orderInfo}}" bindeventTrigger="eventTrigger" tt:if="{{item.type === 'selfLiftInfo'}}" /><block tt:if="{{item.type === 'sku'}}"><view id="skus" /><or skus="{{skus}}" skus-tips="{{skusTips}}" order-id="{{orderId}}" delivery-tips="{{deliveryTips}}" /></block><tm title="{{item.title}}" orderId="{{orderId}}" bindeventTrigger="eventTrigger" tt:if="{{item.title}}" /><ot bindeventTrigger="eventTrigger" item="{{temp}}" isLast="{{idx === temp.data.length - 1}}" orderId="{{orderId}}" orderStatus="{{orderStatus}}" tt:for="{{item.data || []}}" tt:for-item="temp" tt:for-index="idx" tt:key="title" tt:if="{{item.type !== 'selfLiftInfo'}}" /><oy list="{{questionList}}" orderId="{{orderId}}" tt:if="{{item.type === 'cs'}}" /><view class="total-price" tt:if="{{item.type === 'sku'}}"><image class="line" src="https://s3plus.meituan.net/v1/mss_1f3dca8fe8534e6fa640054bf3ad2ba6/grocery-fe-cdn/wx/images/cdn/orderDetailPriceLine.ff92513.png" /><image class="sanjiao" src="https://s3plus.meituan.net/v1/mss_1f3dca8fe8534e6fa640054bf3ad2ba6/grocery-fe-cdn/wx/images/cdn/orderDetailPriceSanjiao.e686512.png" /><view class="title">合计实付</view><view class="price"><view class="tag">￥</view><view class="num">{{totalPay}}</view></view></view></oe>