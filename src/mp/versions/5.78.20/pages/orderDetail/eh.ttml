<ed show="{{show}}" type="bottom" bindclose="close" isShowIpxBar="{{true}}" z-index="104"><view class="item-container"><view class="item {{currentIndex === -1 ? 'init' : currentIndex === item.index ? 'show' : selected[item.index] ? '' : 'hidden'}}" style="{{containerStyle}}" tt:for="{{satisfied.questions}}" tt:key="index"><in bindcancel="close" bindsubmit="submit" idx="{{num}}" selectData="{{selectData}}" bindchangeSelect="changeSelect" question="{{item}}" orderId="{{orderId}}" title="{{satisfied.title}}" count="{{satisfied.count}}" tt:if="{{item.type === 3}}"></in><im bindcancel="close" bindsubmit="submit" bindinput="input" bindnextStep="nextStep" idx="{{num}}" selectData="{{selectData}}" bindchangeSelect="changeSelect" orderId="{{orderId}}" question="{{item}}" title="{{satisfied.title}}" count="{{satisfied.count}}" bindchangeStyle="onChangeStyle" isAndroid="{{isAndroid}}" tt:if="{{item.type === 2}}"></im><oq bindcancel="close" bindsubmit="submit" idx="{{num}}" selectData="{{selectData}}" bindchangeSelect="changeSelect" question="{{item}}" orderId="{{orderId}}" title="{{satisfied.title}}" count="{{satisfied.count}}" tt:if="{{item.type === 1}}"></oq></view></view></ed>