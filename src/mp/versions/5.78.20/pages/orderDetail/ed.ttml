<view class="btn-wrapper pay {{isIpx ? 'ipx' : ''}}" tt:if="{{orderInfo.status === 10 && payBtn}}"><view class="container"><view class="price">实付款:<view class="symbol">¥</view><view class="cashPay">{{orderInfo.cashPay}}</view></view><to type="{{payBtn._btnStyle}}" plain="{{payBtn._isPlain}}" custom-class="btn-pay" data-order-id="{{orderId}}" data-type="{{payBtn.type}}" data-link="{{payBtn.link}}" data-phone="{{customPhone}}" data-extract-info="{{payBtn.extractInfo}}" extendData="{{payBtn}}" catchtap="toOperateOrder">{{payBtn.name}}</to></view></view><view class="btn-wrapper btn-{{size}} {{isIpx ? 'ipx' : ''}}" tt:else><ow list="{{list}}" orderId="{{orderId}}" customPhone="{{customPhone}}" bindtoOperateOrder="operateOrder" tt:if="{{more}}" /><block tt:for="{{list}}" tt:key="name"><rf customer-style="custom-button" bid="{{item.bid}}" lxParams="{{item.lxParams}}" bindmvReport="mvHandler" tt:if="{{!item.more && item.type !== 35 && item.type !== 36}}"><to open-type="{{item.type === 7 || item.type === 22 ? 'share' : ''}}" type="{{item._btnStyle}}" plain="{{item._isPlain}}" custom-class="order-button {{!item._isPlain && !item._btnStyle && 'normal'}} {{item._btnClass}} {{index + 1 === list.length ? 'clear-margin-right' : ''}}" catchtap="toOperateOrder" data-type="{{item.type}}" data-link="{{item.link}}" data-order-id="{{orderId}}" data-phone="{{customPhone}}" data-extract-info="{{item.extractInfo}}" in-group is-last="{{index + 1 === list.length}}" extendData="{{item}}"><image class="wx-image" src="https://p0.meituan.net/travelcube/96e62a7e7e455b262f12a11f4bb5ed351109.png@42w_42h_80q" tt:if="{{item.type === 22 && orderInfo.groupStatus.groupId && orderInfo.groupStatus.isGrouping}}" />{{item.name}}</to></rf></block></view>