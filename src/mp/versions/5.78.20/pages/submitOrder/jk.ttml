<template name="payView"><view class="pay-view pay-view-{{fontRate}}" style="bottom:{{isIpx ? 50 : 0}}rpx;" tt:if="{{orderPreview}}"><view class="fee fee-{{fontRate}}"><view class="total-money"><view class="price-text text submitOrder-28-{{fontRate}}">待支付:</view><view class="price-unit text submitOrder-28-{{fontRate}}">￥</view><view class="price-value text submitOrder-40-{{fontRate}} text-{{fontRate}}">{{orderPreview.cashPay ? orderPreview.cashPay / 100 : 0}}</view></view></view><view class="pay-right"><view class="pay-btn-view" capture-catch:tap="{{(!!orderPreview.priceThresholdStatus || submitDisabled) ? 'disabledSubmit' : ''}}"><button class="template-button" formType="submit"><re type="{{payType === 'oneclickpay' ? 'warn' : 'danger'}}" custom-class="{{(!!orderPreview.priceThresholdStatus || submitDisabled) ? 'pay diable-pay' : 'pay'}}" bindtap="submit" disabled="{{!!orderPreview.priceThresholdStatus || submitDisabled}}" formType="submit">{{submitText}}</re></button></view></view></view><view class="pay-view-placeholder" style="height:{{isIpx ? 158 : 108}}rpx;" tt:if="{{orderPreview}}"></view></template>